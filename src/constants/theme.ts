import { ThemeConfig } from "antd";
const theme: ThemeConfig = {
  token: {
    // Seed Token
    colorPrimary: "#14b8a6"
  },
  components: {
    Typography: {
      colorText: "#fff"
    }
  }
};

export default theme;
